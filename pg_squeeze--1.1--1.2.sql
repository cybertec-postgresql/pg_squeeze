/* pg_squeeze--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION
\echo Use "ALTER EXTENSION pg_squeeze UPDATE TO '1.2'" to load this file. \quit

-- No schema changes are needed for this particular update path.
